repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run pytest
        entry: docker compose exec -T api pytest tests/ -v
        language: system
        pass_filenames: false
        always_run: true
